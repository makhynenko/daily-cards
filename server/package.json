{
  "name": "gql-server",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node ./src/index",
    "deploy": "prisma deploy && prisma generate -e .env"
  },
  "dependencies": {
    "graphql-yoga": "^1.18.2",
    "prisma-client-lib": "^1.34.5"
  }
}
